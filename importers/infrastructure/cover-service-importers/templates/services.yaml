---
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ .Release.Name }}-service
  labels:
    tier: backend
spec:
  selector:
    app: {{ .Release.Name }}-importers
    tier: backend
  ports:
    - protocol: TCP
      port: 80
  type: ClusterIP
